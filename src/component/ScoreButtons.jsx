import React, { Fragment } from "react";
import { Button } from "./Button";

export const ScoreButtons = () => {
  return (
    <Fragment>
      <Button>Point Team Red</Button>
      <Button>Point Team Blue</Button>
    </Fragment>
  );
};
